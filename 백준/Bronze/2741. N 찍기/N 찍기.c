#include <stdio.h>
#include <string.h>    // strlen 함수가 선언된 헤더 파일
#pragma warning(disable : 4996)
int main()
{
	int N;
	scanf("%d", &N);

	for (int i = 1; i <=N; i++)
	{
		
		printf("%d\n", i);



	}
}